# Required
ANTHROPIC_API_KEY=sk-ant-...

# AirOps MCP — live brand kit mode (recommended)
# Claude fetches brand kit 26564 via the AirOps MCP server on every request.
#
# To get your token:
#   1. npx @modelcontextprotocol/inspector
#   2. Transport type: SSE or Streamable HTTP
#   3. URL: https://app.airops.com/mcp
#   4. Click "Open Auth Settings" → "Quick OAuth Flow" → sign in with AirOps
#   5. Copy the access_token value
#
AIROPS_MCP_TOKEN=
AIROPS_BRAND_KIT_ID=26564

# Fallback REST API fetch (used if AIROPS_MCP_TOKEN is not set and you want
# the brand kit refreshed at server startup rather than using the static seed)
AIROPS_API_KEY=

# Optional — defaults to 3001
PORT=3001
