# Required
ANTHROPIC_API_KEY=sk-ant-...

# Optional — fetches live brand kit from AirOps API at startup
# Without this, the server uses the static seed from brand kit ID 26564
AIROPS_API_KEY=
AIROPS_BRAND_KIT_ID=26564

# Optional — defaults to 3001
PORT=3001
